{
  "pre-commit": {
    "parallel": true,
    "jobs": [
      {
        "name": "format all source and header files",
        "files": "git ls-files --cached --others --exclude-standard *.h *.c",
        "run": "clang-format -i {files} -style=file"
      }
    ]
  }
}
